"use strict";
// função escrita normal em typescript
// function retornoAPI(url: string): string {
//     return url;
// }
const retornoAPI = (url) => {
    console.log('URL DA API', url);
};
retornoAPI("http://lucastimoteo.com");
